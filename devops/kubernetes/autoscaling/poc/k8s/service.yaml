apiVersion: v1
kind: Service
metadata:
  name: nodejs-hpa-service
  labels:
    app: nodejs-hpa-poc
spec:
  type: NodePort
  selector:
    app: nodejs-hpa-poc
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
