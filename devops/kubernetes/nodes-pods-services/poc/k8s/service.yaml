apiVersion: v1
kind: Service
metadata:
  name: nodejs-poc-service
  labels:
    app: nodejs-poc
spec:
  type: NodePort
  selector:
    app: nodejs-poc
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
