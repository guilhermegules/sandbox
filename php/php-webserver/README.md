# php-webserver

Webserver developed with pure PHP, from [Writing a webserver in pure PHP](http://station.clancats.com/writing-a-webserver-in-pure-php/)

## Runing

- For install all dependencies `composer install`;
- Run server `sudo php server 8008` on root folder.
