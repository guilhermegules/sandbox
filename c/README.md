# prog-c

Algorithms and study with C language

## How run

- `ctrl + shift + p` select the compiler to build your application in VSCode;
- `ctrl + shift + b` for build our file;
- run `./your-file` will run the C/C++ binary
