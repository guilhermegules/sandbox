#include <stdio.h>
#include <conio.h>
#define tam 100
main(){
	/*4. Escreva um programa que leia dados para um vetor de 100 elementos inteiros. Imprima o maior e o
	menor, sem ordenar, o percentual de pares e a média dos elementos do vetor.	*/
	
	int vet[tam], i, media, perc_par, maior, menor;
	
	for(i=0; i<tam; i++){
		
		if(tam>maior){
			maior = tam;
			printf("Maior Numero %i\n", vet[tam]);
		}
		if(tam<menor){
			menor = tam;
			printf("Menor Numero %i\n", vet[tam]);
		}
		
		
	}
	
}
